<!DOCTYPE html>
<html lang="<%= language === 'zh' ? 'zh-TW' : 'en' %>">
<head>
<!-- meta attributes -->
<meta charset="utf-8">
<meta name="author" content="Jack Gowan">
<meta name="description" content="first built maze">
<title>The Maze - Greg's water colours</title>
<link rel="stylesheet" href="/css/a_maze_style.css">
</head>
<body>
<h1 class="main-title"><%= translations?.[language]?.title || 'The Journey Recounted' %></h1>

  <div class="room-container">
    <div class="content-wrapper">
      <div class="room-image">
        <img src="/images/a5_maze-ghost-3_ext2.png" alt="Continuing the story">
      </div>
      <div class="room-content">
    <div class="room-text">
      <code>
            <%- translations?.[language]?.text || `<p>"This is a self portrait of me when I'm feeling a little angsty." Greg beams.</p>
        <p>"I love it, its great."</p>
        <p>"I've got some more."</p>` %>
          </code>
    </div>

    <hr/>

    <div class="navigation">
          <p><%= translations?.[language]?.choices?.prompt || 'Choose your response:' %></p>
          <a href="/maze/ghost3-ext3"><%= translations?.[language]?.choices?.options?.[0] || 'look at another water colour' %></a>
          <a href="/maze/ghost-4"><%= translations?.[language]?.choices?.options?.[1] || 'break down and tell him all you know' %></a>
          <a href="/maze/ghost-death-3"><%= translations?.[language]?.choices?.options?.[2] || 'Tell him he\'s really weird' %></a>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Detect back button and redirect to start
    window.onpageshow = function(event) {
      if (event.persisted || (performance.navigation && performance.navigation.type === 2)) {
        window.location.replace('/');
      }
    };
  </script>
</body>
</html>